# Шаг 20. Таблица и сервис словаря

🎯 **Цель:** есть таблица `dictionary_lookups` и сервис, который умеет искать слова через OpenAI.

📁 **Файлы шага:**
- SQL для таблицы
- `src/main/java/com/lingua/api/service/DictionaryService.java` (новый)

## 1. Таблица в БД

В словаре будем хранить **массивы** (несколько переводов, синонимов, примеров). PostgreSQL отлично с этим справляется через `text[]`.

В psql:

```sql
CREATE TABLE dictionary_lookups (
    id SERIAL PRIMARY KEY,
    word TEXT NOT NULL,
    translations TEXT[] NOT NULL,
    synonyms TEXT[],
    part_of_speech TEXT,
    transcription TEXT,
    examples TEXT[],
    example_translations TEXT[],
    looked_up_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_dictionary_word ON dictionary_lookups(word);
CREATE INDEX idx_dictionary_looked_up_at ON dictionary_lookups(looked_up_at DESC);
```

🧠 **Почему не делаем `@Entity`?** JPA из коробки плохо умеет с PG-массивами. Сделать можно, но мучительно. Поэтому будем работать через **`JdbcTemplate`** — лёгкую обёртку над JDBC.

## 2. Сервис

**`src/main/java/com/lingua/api/service/DictionaryService.java`** (новый):

```java
package com.lingua.api.service;

import com.fasterxml.jackson.databind.ObjectMapper;
import lombok.RequiredArgsConstructor;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.jdbc.core.RowMapper;
import org.springframework.stereotype.Service;

import java.sql.Array;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.time.Instant;
import java.util.*;

@Service
@RequiredArgsConstructor
public class DictionaryService {

    private final JdbcTemplate jdbc;
    private final OpenAiService openAi;
    private final ObjectMapper mapper = new ObjectMapper();

    private static final RowMapper<Map<String, Object>> LOOKUP_MAPPER = (rs, rowNum) -> {
        Map<String, Object> m = new LinkedHashMap<>();
        m.put("word", rs.getString("word"));
        m.put("translations", toStringList(rs, "translations"));
        m.put("synonyms", toStringList(rs, "synonyms"));
        m.put("partOfSpeech", rs.getString("part_of_speech"));
        m.put("transcription", rs.getString("transcription"));
        m.put("examples", toStringList(rs, "examples"));
        m.put("exampleTranslations", toStringList(rs, "example_translations"));
        m.put("lookedUpAt", rs.getTimestamp("looked_up_at").toInstant().toString());
        return m;
    };

    private static List<String> toStringList(ResultSet rs, String column) throws SQLException {
        Array arr = rs.getArray(column);
        if (arr == null) return new ArrayList<>();
        String[] strings = (String[]) arr.getArray();
        return strings != null ? Arrays.asList(strings) : new ArrayList<>();
    }

    public Map<String, Object> lookup(String word, String context) {
        String normalizedWord = word.toLowerCase().trim();

        // 1. Кэш
        List<Map<String, Object>> cached = jdbc.query(
                "SELECT * FROM dictionary_lookups WHERE word = ? ORDER BY looked_up_at DESC LIMIT 1",
                LOOKUP_MAPPER, normalizedWord);

        if (!cached.isEmpty()) {
            jdbc.update("UPDATE dictionary_lookups SET looked_up_at = NOW() WHERE word = ? AND looked_up_at = (SELECT MAX(looked_up_at) FROM dictionary_lookups WHERE word = ?)",
                    normalizedWord, normalizedWord);
            Map<String, Object> entry = cached.get(0);
            entry.put("lookedUpAt", Instant.now().toString());
            return entry;
        }

        // 2. Запрос к OpenAI
        String contextHint = (context != null && !context.isBlank())
                ? "The word appears in this sentence: \"" + context + "\""
                : "";

        String systemPrompt = """
                You are an English–Russian dictionary. Respond ONLY with a JSON object — no markdown.

                JSON format:
                {
                  "word": "word",
                  "translations": ["перевод1", "перевод2"],
                  "synonyms": ["synonym1", "synonym2"],
                  "partOfSpeech": "noun|verb|adjective|...",
                  "transcription": "/AmE IPA/",
                  "examples": ["English example 1.", "English example 2."],
                  "exampleTranslations": ["Русский перевод 1.", "Русский перевод 2."]
                }
                """;

        List<Map<String, String>> messages = List.of(
                Map.of("role", "system", "content", systemPrompt),
                Map.of("role", "user", "content", "Word to look up: \"" + word + "\"\n" + contextHint)
        );

        String content = openAi.complete("gpt-4.1-nano", 600, messages);
        content = content.replaceAll("```json\\n?|\\n?```", "").trim();

        Map<?, ?> ai;
        try {
            ai = mapper.readValue(content, Map.class);
        } catch (Exception e) {
            ai = Map.of("translations", List.of("перевод недоступен"), "examples", List.of());
        }

        String resultWord = ai.get("word") != null ? ai.get("word").toString().toLowerCase().trim() : normalizedWord;
        List<String> translations = toList(ai.get("translations"), "перевод недоступен");
        List<String> synonyms = toList(ai.get("synonyms"), null);
        String partOfSpeech = ai.get("partOfSpeech") != null ? ai.get("partOfSpeech").toString() : null;
        String transcription = ai.get("transcription") != null ? ai.get("transcription").toString() : null;
        List<String> examples = toList(ai.get("examples"), null);
        List<String> exampleTranslations = toList(ai.get("exampleTranslations"), null);

        // 3. Сохраняем в кэш
        jdbc.update(con -> {
            var ps = con.prepareStatement(
                    "INSERT INTO dictionary_lookups (word, translations, synonyms, part_of_speech, transcription, examples, example_translations) VALUES (?, ?, ?, ?, ?, ?, ?)");
            ps.setString(1, resultWord);
            ps.setArray(2, con.createArrayOf("text", translations.toArray(String[]::new)));
            ps.setArray(3, con.createArrayOf("text", synonyms.toArray(String[]::new)));
            ps.setString(4, partOfSpeech);
            ps.setString(5, transcription);
            ps.setArray(6, con.createArrayOf("text", examples.toArray(String[]::new)));
            ps.setArray(7, con.createArrayOf("text", exampleTranslations.toArray(String[]::new)));
            return ps;
        });

        Map<String, Object> result = new LinkedHashMap<>();
        result.put("word", resultWord);
        result.put("translations", translations);
        result.put("synonyms", synonyms);
        result.put("partOfSpeech", partOfSpeech);
        result.put("transcription", transcription);
        result.put("examples", examples);
        result.put("exampleTranslations", exampleTranslations);
        result.put("lookedUpAt", Instant.now().toString());
        return result;
    }

    public List<Map<String, Object>> recent() {
        return jdbc.query(
                "SELECT * FROM dictionary_lookups ORDER BY looked_up_at DESC LIMIT 20",
                LOOKUP_MAPPER);
    }

    @SuppressWarnings("unchecked")
    private List<String> toList(Object value, String fallback) {
        if (value instanceof List<?> list) {
            return list.stream().map(Object::toString).toList();
        }
        return fallback != null ? List.of(fallback) : new ArrayList<>();
    }
}
```

🧠 **Разбор главных моментов:**

### `JdbcTemplate`

Spring **сам** создаст этот бин на базе нашего `DataSource`. Просто инжектим.

### `RowMapper`

```java
private static final RowMapper<Map<String, Object>> LOOKUP_MAPPER = (rs, rowNum) -> {
    Map<String, Object> m = new LinkedHashMap<>();
    m.put("word", rs.getString("word"));
    ...
};
```

Это лямбда: «как из строки `ResultSet` сделать `Map`». Используется для каждой строки результата запроса.

### Чтение PG-массива

```java
Array arr = rs.getArray("translations");
String[] strings = (String[]) arr.getArray();
```

`rs.getArray(...)` возвращает JDBC-обёртку. `.getArray()` — настоящий Java-массив.

### INSERT с массивами

```java
jdbc.update(con -> {
    var ps = con.prepareStatement("INSERT ...");
    ps.setArray(2, con.createArrayOf("text", translations.toArray(String[]::new)));
    return ps;
});
```

Для PG-массивов нужно создать `Array` из соединения через `con.createArrayOf("text", ...)`. Поэтому используем форму с `PreparedStatementCreator`.

### `var` и text block

- `var ps = ...` — короткое объявление с выводом типа (Java 10+).
- `"""..."""` — text block, многострочная строка без escape (Java 15+).

### Алгоритм `lookup`

1. Проверяем кэш по слову.
2. Нет → зовём OpenAI.
3. Парсим JSON-ответ.
4. Сохраняем в кэш.
5. Возвращаем.

## ⚠️ Что мы упростили

В `artifacts/api-server/` `systemPrompt` подлиннее и содержит дополнительные правила:
- если запрашиваемое слово — часть фразового глагола (например, `put` в «put up with»), вернуть **полную** фразу;
- список `synonyms` должен совпадать по длине с `translations` (по одному синониму на каждый перевод).

Также после получения ответа `synonyms` искусственно «добиваются» пустыми строками до длины `translations`. Можешь добавить эти правила в свою версию, сравнив с эталоном — мы оставили базовую логику, чтобы шаг был легче.

## ✅ Проверка

Эндпоинта пока нет, но компиляция должна пройти. Перезапусти сервер — должно работать без ошибок.

## 🤔 Проверь себя

  1. Когда удобнее использовать `JdbcTemplate`, а не Spring Data JPA?
  2. Что такое `PG-массив` и почему JPA сам с ним не справляется?
  3. Зачем нам кэш в БД для словарных переводов — почему не вызывать OpenAI каждый раз?

  <details>
  <summary>Мини-упражнение: ключ кэша</summary>

  Сейчас ключ кэша — это просто `word` (одна колонка в таблице). Что произойдёт, если пользователь спросит «put» с контекстом «I can put up with this»? Должен ли контекст быть частью ключа?

  **Ответ:** в нашем случае — **нет**. Мы храним базовую форму слова или фразового глагола. Контекст помогает OpenAI выбрать правильное значение, но результат кэшируется по итоговому слову. Если бы мы хотели разные переводы для разных контекстов — пришлось бы добавить колонку `context_hash` в таблицу и в ключ.
  </details>

  ## ➡️ Дальше

Делаем эндпоинты словаря. [Шаг 21 — Эндпоинты словаря →](21-словарь-эндпоинты.md)
