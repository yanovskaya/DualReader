# Шаг 14. Статистика книги

🎯 **Цель:** работают `GET /books/:id/stats` и `GET /books/:id/translation-status`.

📁 **Файлы шага:**
- `src/main/java/com/lingua/api/repository/BookRepository.java`
- `src/main/java/com/lingua/api/service/BookService.java`
- `src/main/java/com/lingua/api/controller/BookController.java`

## 1. Считаем слова в БД

Подсчёт слов в SQL — отличный кейс для `nativeQuery`. Используем PG-функции `regexp_split_to_array` и `array_length`.

В `BookRepository.java` добавь импорт:
```java
import org.springframework.data.jpa.repository.Query;
```

И метод:

```java
@Query(value = "SELECT SUM(array_length(regexp_split_to_array(trim(original_text), '\\s+'), 1)) FROM paragraphs WHERE book_id = :bookId", nativeQuery = true)
Long countWordsByBookId(Integer bookId);
```

🧠 **Разница JPQL vs Native:**
- JPQL — кросс-БД, оперирует именами классов и полей.
- Native — пишем чистый SQL для PostgreSQL.

`nativeQuery = true` — обязательно, иначе JPA попробует распарсить это как JPQL и упадёт.

## 2. Метод `getStats`

В `BookService.java`:

```java
public Map<String, Object> getStats(Integer bookId) {
    Book book = bookRepo.findById(bookId).orElseThrow();
    Long wordCount = bookRepo.countWordsByBookId(bookId);
    int wc = wordCount != null ? wordCount.intValue() : 0;
    int pct = book.getTotalParagraphs() > 0
            ? Math.round((float) book.getTranslatedParagraphs() / book.getTotalParagraphs() * 100)
            : 0;

    Map<String, Object> result = new LinkedHashMap<>();
    result.put("bookId", book.getId());
    result.put("totalParagraphs", book.getTotalParagraphs());
    result.put("translatedParagraphs", book.getTranslatedParagraphs());
    result.put("wordCount", wc);
    result.put("uniqueWordsLookedUp", 0);  // заглушка: реальный счётчик появится после Шага 20 (словарь).
    result.put("progressPercent", pct);
    return result;
}
```

🧠 `bookRepo.findById(bookId).orElseThrow()` — если книги нет, бросит `NoSuchElementException` (Spring вернёт 500). Мы заранее проверяем существование в контроллере, так что сюда дойдём только если книга есть.

## 3. Эндпоинты

В `BookController.java`:

```java
@GetMapping("/books/{id}/stats")
public ResponseEntity<?> getStats(@PathVariable Integer id) {
    if (bookService.getBook(id).isEmpty()) {
        return ResponseEntity.notFound().build();
    }
    return ResponseEntity.ok(bookService.getStats(id));
}

@GetMapping("/books/{id}/translation-status")
public ResponseEntity<?> translationStatus(@PathVariable Integer id) {
    return bookService.getBook(id).map(book -> {
        int pct = book.getTotalParagraphs() > 0
                ? Math.round((float) book.getTranslatedParagraphs() / book.getTotalParagraphs() * 100)
                : 0;
        Map<String, Object> result = new LinkedHashMap<>();
        result.put("bookId", book.getId());
        result.put("status", book.getTranslationStatus());
        result.put("totalParagraphs", book.getTotalParagraphs());
        result.put("translatedParagraphs", book.getTranslatedParagraphs());
        result.put("progressPercent", pct);
        return ResponseEntity.ok(result);
    }).orElse(ResponseEntity.notFound().build());
}
```

## ✅ Проверка

```bash
curl http://localhost:8080/books/6/stats
curl http://localhost:8080/books/6/translation-status
```

В ответе `wordCount` = подсчитанное количество слов, `progressPercent` = 0 (мы ещё не переводили).

## 🤔 Проверь себя

  1. Когда стоит использовать `nativeQuery = true` вместо JPQL?
  2. Почему мы возвращаем `Map<String, Object>`, а не отдельный класс DTO?
  3. Что делают PG-функции `regexp_split_to_array(text, '\s+')` и `array_length(arr, 1)` — как они вместе считают слова?

  <details>
  <summary>Мини-упражнение: добавь процент перевода</summary>

  В ответ статистики добавь поле `translatedPercent`: `(translated / total) * 100`, округлённое до целого. Не забудь про деление на ноль, если `total = 0`.
  </details>

  ## ➡️ Дальше

Теперь дадим возможность загружать файлы. [Шаг 15 — Загрузка .txt →](15-загрузка-txt.md)
