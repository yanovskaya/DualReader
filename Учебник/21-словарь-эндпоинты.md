# Шаг 21. Эндпоинты словаря

🎯 **Цель:** работают `GET /dictionary/lookup?word=...` и `GET /dictionary/recent`.

📁 **Файлы шага:**
- `src/main/java/com/lingua/api/controller/DictionaryController.java` (новый)

## 1. Контроллер

**`src/main/java/com/lingua/api/controller/DictionaryController.java`** (новый):

```java
package com.lingua.api.controller;

import com.lingua.api.service.DictionaryService;
import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;
import java.util.Map;

@RestController
@RequestMapping("/dictionary")
@RequiredArgsConstructor
public class DictionaryController {

    private final DictionaryService dictionaryService;

    @GetMapping("/lookup")
    public ResponseEntity<?> lookup(
            @RequestParam(required = false) String word,
            @RequestParam(required = false) String context) {
        if (word == null || word.isBlank()) {
            return ResponseEntity.badRequest().body(Map.of("error", "Missing required parameter: word"));
        }
        try {
            return ResponseEntity.ok(dictionaryService.lookup(word, context));
        } catch (Exception e) {
            return ResponseEntity.internalServerError().body(Map.of("error", "Dictionary lookup failed"));
        }
    }

    @GetMapping("/recent")
    public ResponseEntity<List<Map<String, Object>>> recent() {
        return ResponseEntity.ok(dictionaryService.recent());
    }
}
```

🧠 **Что нового:**

### `@RequestMapping("/dictionary")` на классе

Базовый префикс для **всех** методов класса. Получается:
- `@GetMapping("/lookup")` → `GET /dictionary/lookup`
- `@GetMapping("/recent")` → `GET /dictionary/recent`

### Обработка ошибок OpenAI

```java
try {
    return ResponseEntity.ok(dictionaryService.lookup(word, context));
} catch (Exception e) {
    return ResponseEntity.internalServerError().body(Map.of("error", "Dictionary lookup failed"));
}
```

Если OpenAI недоступен — возвращаем 500 с понятной ошибкой, а не «непонятный stack trace в консоль».

## ✅ Проверка

```bash
# Поиск нового слова
curl 'http://localhost:8080/dictionary/lookup?word=resilient'

# Поиск с контекстом
curl 'http://localhost:8080/dictionary/lookup?word=put&context=I+can+put+up+with+this'

# Без word
curl -i 'http://localhost:8080/dictionary/lookup'

# Недавние
curl http://localhost:8080/dictionary/recent
```

Ожидаем:
- Первый — JSON со словом, переводами, примерами.
- Второй — `word` будет `"put up with"` (фразовый глагол распознан).
- Третий — 400.
- Четвёртый — массив последних поисков.

Повтори первый запрос — должен вернуться **мгновенно** (из кэша).

## 🤔 Проверь себя

  1. Зачем у нас `@RequestMapping("/dictionary")` на классе, если можно писать полный путь в каждом методе?
  2. Что плохого в том, чтобы пробросить наружу настоящий stacktrace OpenAI?
  3. Какой HTTP-статус правильнее для «нет параметра `word`» — 400 или 422?

  <details>
  <summary>Мини-упражнение: добавь rate-limit (концепция)</summary>

  Что произойдёт, если бот будет слать 1000 запросов в секунду на `/dictionary/lookup` с уникальными словами? Какие три места могут не выдержать?

  **Ответ:** (1) OpenAI — счёт вырастет; (2) база — много `INSERT`-ов; (3) поток-обработчик HTTP — Tomcat-пул переполнится. Решения: rate-limit по IP (`bucket4j`), очередь, валидация «слово ли это».
  </details>

  ## ➡️ Дальше

Финальные штрихи. [Шаг 22 — CORS и финал →](22-финал.md)
